# Base URL for the Lineage Marshal agent API (Issue #12).
# Leave empty to use the dev-server proxy at /api (see vite.config.ts).
VITE_API_BASE_URL=

# Link out to the local DataHub UI from asset rows.
VITE_DATAHUB_UI_URL=http://localhost:9002
